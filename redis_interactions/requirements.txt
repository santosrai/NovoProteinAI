# Redis vector-search ingestion pipeline
redis>=5.0
redisvl>=0.4.0
python-dotenv>=1.0

# PDF extraction. PyMuPDF gives layout-aware (reading-order) text for
# multi-column journal PDFs; pypdf is kept as a fallback.
PyMuPDF>=1.24
pypdf>=4.0

# Semantic chunking + embeddings (EmbeddingGemma, open weights).
# EmbeddingGemma requires sentence-transformers>=5.0 and transformers>=4.56.
sentence-transformers>=5.0
transformers>=4.56
numpy>=1.24

# Authenticating to / downloading the gated EmbeddingGemma model from the
# Hugging Face Hub (provides the `hf` CLI used for `hf auth login`).
huggingface_hub>=0.34

# PubMed/PMC ingestion (research_paper_interactions) fetches papers over HTTP
# and parses the XML with the stdlib (xml.etree); only `requests` is extra.
requests>=2.31
