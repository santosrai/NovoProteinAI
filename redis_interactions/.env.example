# --- Redis connection (required) ---
REDIS_HOST=YOUR_HOST
REDIS_PORT=YOUR_PORT
REDIS_USERNAME=default
REDIS_PASSWORD=YOUR_PASSWORD
# Set to true for managed Redis that requires TLS (connects over rediss://).
REDIS_TLS=false

# --- Embedding model (optional; defaults shown) ---
# Must match the index dimensionality below.
EMBEDDING_MODEL=google/embeddinggemma-300m
EMBEDDING_DIM=768

# --- Vector index (optional; defaults shown) ---
REDIS_INDEX_NAME=idx:papers
REDIS_KEY_PREFIX=chunk:
